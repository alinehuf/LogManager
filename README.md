# LogManager
**LogManager** is a plugin that enables asynchronous logging in Unreal Engine 5.6 (Windows/Android) in the form of JSON files. The goal is to provide a tool for researchers who want to log a large amount of data during an experiment without slowing down Unreal’s frame rate.
